# Environment: development, staging, production
ENVIRONMENT=development

# Set the allowed origins for CORS, comma-separated URLs (no quotes needed for BaseSettings usually)
# Example for local Vite dev server (default port 5173) and standard HTTP ports:
BACKEND_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost,http://127.0.0.1

# Base API prefix
API_PREFIX=/api

# Project Name (Optional)
PROJECT_NAME="Modular Dashboard API (Dev)"

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL) - Use level names or numbers
# LOGGING_LEVEL=INFO