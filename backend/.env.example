# Environment: development, staging, production
ENVIRONMENT=development

# Set the allowed origins for CORS as a JSON array string
BACKEND_CORS_ORIGINS='["http://localhost:5173","http://127.0.0.1:5173","http://localhost","http://127.0.0.1"]'

# Base API prefix
API_PREFIX=/api

# Project Name (Optional)
PROJECT_NAME="Modular Dashboard API (Dev)"

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOGGING_LEVEL=INFO

# --- API Keys ---
# API Key for Google AI Services (e.g., Gemini)
GOOGLE_API_KEY= # Add your key ONLY in the .env file, NOT here.

# --- Model Configuration ---
# Specific Gemini model to use (e.g., gemini-1.5-flash-latest, gemini-2.0-flash-exp)
GEMINI_MODEL_NAME=gemini-2.0-flash-exp